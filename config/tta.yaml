start_new_exp: False
wandb_log: True
dataset: hcp_t2   # brain: hcp_t1, hcp_t2, abide_caltech, abide_stanford; wmh: nuhs, umc, vu
split:   val       # val, test
start: null       # Index of the test dataset to start at
stop:  null       # Index of the test dataset to stop at
num_workers: 0
device: cuda
tta_mode: dae_and_diffusion # no_tta, gnn, gt, dae, dae_and_diffusion
image_size: [1, 256, 256]
seed: 0

bg_suppression_opts:
  type: fixed_value  # possible values: none, fixed_value, random_value
  bg_value: -0.5
  bg_value_min: -0.5
  bg_value_max: 1
  mask_source: thresholding  # possible values: thresholding, ground_truth
  thresholding: otsu  # possible values: isodata, li, mean, minimum, otsu, triangle, yen
  hole_filling: True

dae:
  wandb_project: mt-tta-dae-dae_tta
  logdir: /scratch_net/biwidl319/jbermeo/logs/brain/sota/dae/hcp_t1_bg_supp_bs_32_no_grad_accumulation
  dae_dir: /scratch_net/biwidl319/jbermeo/data/models/brain/dae/hcp_t1
  seg_dir: /scratch_net/biwidl319/jbermeo/data/models/brain/segmentation/hcp_t1/bg_supp
  batch_size: 32
  dataset_repetition: 1
  num_steps: 500  # 500 for brain, 2700 for wmh
  update_dae_output_every: 25
  calculate_dice_every: 25  # 25 for brain, 135 for wmh (nuhs, umc, vu)
  alpha: 1.0
  beta: 0.25
  learning_rate: 0.001
  save_checkpoints: True
  const_aug_per_volume: False
  accumulate_over_volume: False

  bg_suppression_opts:
    type: fixed_value  # possible values: none, fixed_value, random_value
    bg_value: -0.5
    bg_value_min: -0.5
    bg_value_max: 1
    mask_source: thresholding  # possible values: thresholding, ground_truth
    thresholding: otsu  # possible values: isodata, li, mean, minimum, otsu, triangle, yen
    hole_filling: True
  
  augmentation:
    da_ratio: 0.25
    sigma: 20
    alpha: 0
    trans_min: 0
    trans_max: 0
    rot_min: 0
    rot_max: 0
    scale_min: 1.0
    scale_max: 1.0
    gamma_min: 0.5
    gamma_max: 2.0
    brightness_min: 0.0
    brightness_max: 0.1
    noise_mean: 0.0
    noise_std: 0.1


dae_and_diffusion:
  wandb_project: mt-tta-dae_and_diffusion_tta
  logdir: /scratch_net/biwidl319/jbermeo/logs/brain/sota/dae_and_ddpm/2_19/ddpm(1.0)_and_atlas(0.5)_lr_1em3_no_grad_acc_gt_new_ddpm_y_atlas
  dae_dir: /scratch_net/biwidl319/jbermeo/data/models/brain/dae/hcp_t1
  seg_dir: /scratch_net/biwidl319/jbermeo/data/models/brain/segmentation/hcp_t1/bg_supp
  ddpm_dir: /scratch_net/biwidl319/jbermeo/logs/brain/ddpm/not_one_hot_64_base_filters_with_aug_except_noise
  cpt_fn: 'model-9.pt' # or 23, 28, or 31

  dddpm_loss_beta: 1.0
  dae_loss_alpha: 0.5

  frac_vol_diffusion_tta: 1.0
  min_t_diffusion_tta: 0
  max_t_diffusion_tta: 1000
  sampling_timesteps: 100
  min_max_int_norm_imgs: [-0.5313728, 0.67584044]
  use_y_pred_for_ddpm_loss: False
  use_x_cond_gt: False    # Only true for debugging

  batch_size: 32
  dataset_repetition: 1
  num_steps: 2000  # 500 for brain, 2700 for wmh
  update_dae_output_every: 25
  calculate_dice_every: 25  # 25 for brain, 135 for wmh (nuhs, umc, vu)
  alpha: 1.0
  beta: 1.25 # 0.25
  learning_rate: 0.001
  accumulate_over_volume: False
  save_checkpoints: True
  const_aug_per_volume: False
  
  bg_suppression_opts:
    type: fixed_value  # possible values: none, fixed_value, random_value
    bg_value: -0.5
    bg_value_min: -0.5
    bg_value_max: 1
    mask_source: thresholding  # possible values: thresholding, ground_truth
    thresholding: otsu  # possible values: isodata, li, mean, minimum, otsu, triangle, yen
    hole_filling: True
  
  augmentation:
    da_ratio: 0.25
    sigma: 20
    alpha: 0
    trans_min: 0
    trans_max: 0
    rot_min: 0
    rot_max: 0
    scale_min: 1.0
    scale_max: 1.0
    gamma_min: 0.5
    gamma_max: 2.0
    brightness_min: 0.0
    brightness_max: 0.1
    noise_mean: 0.0
    noise_std: 0.0
